# ── Local development (these are already the defaults, only needed if you change them) ──
# VITE_WS_URL=ws://localhost:1999

# ── Production (set this in Vercel/Netlify env vars dashboard) ──
# VITE_WS_URL=wss://your-app.up.railway.app
